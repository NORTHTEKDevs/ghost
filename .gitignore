/target
.vscode/
.idea/
*.iml
.DS_Store
Thumbs.db
*.log
diag_*.png
dist/
public/ghost_*.png
__pycache__/
*.pyc
notepad_test_screenshot.png

# isolated build dir used when target/release is locked by a running ghost-mcp
target-kit/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# Side build directories (e.g. target-cert/ when target/release is locked by a running server)
target-*/
