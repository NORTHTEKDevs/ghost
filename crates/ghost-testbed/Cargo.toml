[package]
name = "ghost-testbed"
version = "0.1.0"
edition = "2021"
description = "A tiny Win32 window with an edit box and buttons: the deterministic target for Ghost's live tests"
license = "MIT"
publish = false

[[bin]]
name = "ghost-testbed"
path = "src/main.rs"

[target.'cfg(windows)'.dependencies]
windows = { workspace = true }
